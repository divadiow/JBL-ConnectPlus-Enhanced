# Test APKs

Debug APKs published by the `Android CI` workflow are stored in this directory after a successful build of `main`.

These are development builds signed with GitHub Actions' generated Android debug key. They may require uninstalling a previous debug build if its signing key differs.
